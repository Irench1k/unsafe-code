# @import ../_imports.http
# @import ../patch/happy.http

# GET /orders/{id}/refund/status authorization guardrails
# Customer-only endpoint - verify customers can only see their own refunds

### Customer without order ownership cannot view refund
# @forceRef refund_approved
# @tag authz, orders, r03, v301
GET /orders/{{order_id}}/refund/status
Authorization: {{auth.basic("squidward")}}

?? js $(response).isError() == true
?? js $(response).field("error") != null
