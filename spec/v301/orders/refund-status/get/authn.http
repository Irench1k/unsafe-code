# @import ../_imports.http
# @import ../patch/happy.http

# GET /orders/{id}/refund/status authentication guardrails
# Customer-only endpoint - restaurant API keys are NOT valid auth

### Unauthenticated request fails
# @forceRef refund_approved
# @tag authn, orders, r03, v301
GET /orders/{{order_id}}/refund/status

?? js $(response).isError() == true

### Restaurant API key is not valid auth for this endpoint
# @forceRef refund_approved
# @tag authn, orders, r03, v301
GET /orders/{{order_id}}/refund/status
X-API-Key: {{auth.restaurant("krusty_krab")}}

?? js $(response).isError() == true
