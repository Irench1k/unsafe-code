# @import ../_imports.http
# @import ../../refund/post/happy.http

# PATCH /orders/{id}/refund/status authentication guardrails
# Requires authenticated restaurant manager

### Refund status change requires authentication
# @forceRef large_refund_pending
# @tag authn, orders, r03, v301
PATCH /orders/{{order_id}}/refund/status
Content-Type: application/x-www-form-urlencoded

status=approved

?? js $(response).isError() == true
