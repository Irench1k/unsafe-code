# @import ../_imports.http
# @import ../../../cart/checkout/post/happy.http

# GET /orders multi-tenant data isolation (v301+)
# Different restaurants see only their own orders

### Chum Bucket sees only their restaurant's orders
# @forceRef new_order
# @tag orders, r03, v301
GET /orders
X-API-Key: {{auth.restaurant("chum_bucket")}}

?? js $(response).isOk() == true
?? js $(response).every(o => o.restaurant_id === 2) == true
