# @import ../_imports.http
# @import ../../../cart/checkout/post/happy.http

# GET /orders authentication guardrails
# Listing orders requires authentication

### Orders list requires authentication
# @forceRef new_order
# @tag authn, orders, r02, v201
GET /orders

?? js $(response).isError() == true
