# @import ../_imports.http

# GET /menu happy path
# List all menu items (no restaurant filter in v205)
#
# v205 differences from v301:
#   - No restaurant_id filter parameter
#   - No availability field in items
#   - Single restaurant model

### List menu items (no auth required)
# @name menu_list
{{
  await platform.reset();
}}
# @tag happy, menu, r02, v205
GET /menu

?? js $(response).status() == 200
?? js $(response).count() > 0
