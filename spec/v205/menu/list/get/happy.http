# @import ../_imports.http

# GET /menu happy path
# List all menu items (public access)

### Menu is publicly accessible (no auth required)
# @tag happy, menu, r02, v205
GET /menu

?? js $(response).isOk() == true
?? js $(response).count() > 0
