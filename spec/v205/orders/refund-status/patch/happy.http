# @import ../_imports.http
# @import ../../refund/post/happy.http

# PATCH /orders/{id}/refund/status happy path
# Update refund status (restaurant manager)
#
# Note: Requires restaurant API key authentication

### Approve refund with restaurant API key
# @name refund_approved
# @forceRef refund_created
# @tag happy, orders, r02, v205
PATCH /orders/{{order_id}}/refund/status
X-API-Key: {{auth.restaurant("krusty_krab")}}
Content-Type: application/x-www-form-urlencoded

status=approved

?? js $(response).status() == 200
?? js $(response).fieldEquals("status", "approved") == true
