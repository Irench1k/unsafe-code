# v205 - Session Overwrite Vulnerability Demo
# Authentication Confusion example - simpler in-memory model
#
# API Characteristics:
#   - Single restaurant (no restaurant_id parameter)
#   - Email-based user IDs (strings, not integers)
#   - In-memory storage (Pydantic models, not PostgreSQL)
#   - Session-based auth via cookies
#
# This is the BASE version for the confusion tutorial series.
# v206 and v301+ inherit from this and add complexity.

version: v205
base_url: http://localhost:8000/api/v205

# No parent - this is the foundation
# parent: null

tags:
  - v205
  - r02  # r02_authentication_confusion
